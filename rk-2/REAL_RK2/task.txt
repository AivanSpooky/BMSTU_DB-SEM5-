1) SELECT, использующая поисковое выражение CASE
2) UPDATE со скалярным подзапросом в предложении SET
3) SELECT консолидирующий данные с помощью GROUP BY и HAVING

создать хранимую процедуру без параметров, которая осуществляет поиск ключевого слова 'EXEC' в тексте хранимых процедур в текущей БД. Хранимая процедура выводит инструкцию 'EXEC' которая выполняет хранимую процедуру или скалярную пользовательскую функцию. Созданную процедуру протестировать

все на mssql
в отношении 1 ко многим сделано в таблице поле foreign key ссылающее на другую таблицу
в отношении многие ко многим сделано через доп таблицу-связку со своим ID и двумя foreign key на таблицы
вставку данных в таблице сделано с помощью insert по 10 записей на каждую таблицу
все названия таблиц/переменных/данных на английском языке
запросы или процедуры с комментариями о том, что они делают
запросы осмысленные и логические